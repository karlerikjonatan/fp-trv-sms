name: cron-job
on:
  schedule:
    - cron: "0 8-23 * * *"
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - run: |
          curl --request GET \
          --url '${{ secrets.NETLIFY_API_ENDPOINT }}'
